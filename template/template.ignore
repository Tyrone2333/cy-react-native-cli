# 这是一行注释

packageMap.js
template.ignore

# react-native-syan-image-picker 相关文件夹不输出
<% if (!dependencies.includes('react-native-syan-image-picker')) { %>
    src/views/TakePhoto
<% } %>

# react-native-syan-image-picker 相关文件夹不输出
<% if (!dependencies.includes('react-native-general-actionsheet')) { %>
    src/components/ActionSheetUploadSample.js
<% } %>

dynamicPathFile/
